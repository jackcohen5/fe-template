{
    "roots": ["<rootDir>/src"],
    "testRegex": "((\\.|/*.)(test))\\.ts$",
    "modulePaths": ["<rootDir>/src"],
    "setupFilesAfterEnv": ["<rootDir>/config/setupJest.ts"],
    "transform": {
        "^.+\\.ts?$": "ts-jest",
    }
}
